import type { Metadata } from "next";

export const metadata: Metadata = {
	title: "Expenses",
	description: "Business expenses dashboard",
};

export default function ExpensePage() {
	return <div>ExpenseTablePage</div>;
}
