import type { Metadata } from "next";

export const metadata: Metadata = {
	title: "Account",
	description: "Business account dashboard",
};

export default function AccountPage() {
	return <div>AccountPage</div>;
}
